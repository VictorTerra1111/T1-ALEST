#include <iostream>

using namespace std;


/*
Usando como ponto de partida a classe IntStack, apresentada anteriormente e
implementada usando alocação sequencial ou contígua, implemente a classe
IntLinkedStack, que funciona da mesma forma, porém usando alocação encadeada.

Observações:

    A versão usando alocação encadeada eliminará a necessidade de se trabalhar com um limite
    máximo de elementos, consequentemente, os métodos maxSize() e isFull() NÃO farão
    parte da nova implementação.

    A definição da classe (arquivo IntLinkedStack.hpp) e um programa de teste (arquivo
    IntLinkedStackMain.cpp) estão listados nas lâminas a seguir.

*/
